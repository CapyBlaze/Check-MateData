/* eslint-disable @typescript-eslint/no-unused-vars */



export function decryptFile(
    file: File,
    onProgress?: (remaining: number) => void

): Uint8Array {
    const outputDataFile: Uint8Array = new Uint8Array();

    return outputDataFile;
}